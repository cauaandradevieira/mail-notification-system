spring:
  application:
    name: email-notification-system

  rabbitmq:
    host: localhost
    password: admin
    username: admin
    port: 5672

  mail:
    host: smtp.gmail.com
    password: hjygtlnxjclbeslj
    username: cavclahs@gmail.com
    port: 587

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:h2:~/test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

